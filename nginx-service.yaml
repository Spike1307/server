apiVersion: v1
kind: Service
metadata:
  name: tileserver-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: tileserver
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
